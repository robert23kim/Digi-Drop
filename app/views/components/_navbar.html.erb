<nav class="navbar navbar-expand-lg navbar-dark digiid-bar">
  <a class="navbar-brand digiid-bar-brand" href=<%= users_path %>>
    <h1 class="digiid-logo">
      My Collectibles
    </h1>
  </a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= 'active' if current_page?(users_path) %>">
        <a class="nav-link digiid-link" href=<%= users_path %>> Home </a>
      </li>
      <% if logged_in? %>
        <li class="nav-item <%= 'active' if current_page?(user_path(current_user)) %>">
          <a class="nav-link digiid-link" href=<%= user_path(current_user) %>>Manage</a>
        </li>
        <li class="nav-item <%= 'active' if current_page?(open_path(current_user)) %>">
          <a class="nav-link digiid-link" href=<%= open_path(current_user) %>>Cases</a>
        </li>
        <li class="nav-item <%= 'active' if current_page?(market_user_path(current_user)) %>">
          <a class="nav-link digiid-link" href=<%= market_user_path(current_user) %>>Market</a>
        </li>
      <% end %>
    </ul>
  </div>

  <ul class="navbar-nav mr-auto">
    <% if logged_in? %>
      <li class="nav-item dropdown">
        <a class="nav-link digiid-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Balance: <%= @userBal %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href=<%= add_balance_user_path(current_user) %>>Add Balance</a>
        </div>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link digiid-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= current_user.username %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href=<%= logout_path %>>Log out</a>
        </div>
      </li>
    <% else %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Not logged in
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href=<%= login_path %>>Log in</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href='users/new'>Sign up</a>
        </div>
      </li>
    <% end %>
  </ul>

</nav>